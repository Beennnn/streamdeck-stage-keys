# Built outputs ARE committed in this content repo (icons/, icons.json, dist/,
# duo/*.png). Only ignore incidental artifacts.
contact-sheet.png
# intermediate SVGs for split buttons — regenerable from bin/gen_duo.py
duo/svg/
__pycache__/
*.pyc
.DS_Store

# Icon Pack Man staging (drag these into iconpackman.elgato.com)
iconpackman-drop/
iconpackman-thumbnail.svg

# Maker Console upload assets (generated)
maker-media/
